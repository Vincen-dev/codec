name: codec
description: "类型安全、可组合的 JSON 编解码库:失败抛结构化 CodecException 携带 $.path 路径定位(可选兼容 FormatException),支持嵌套对象、discriminated union、递归结构与多版本兼容。"
version: 0.2.0
repository: https://github.com/Vincen-dev/codec
issue_tracker: https://github.com/Vincen-dev/codec/issues

topics:
  - json
  - codec
  - serialization
  - deserialization

environment:
  sdk: ^3.6.0

resolution: workspace

dev_dependencies:
  test: ^1.25.0
  lints: ^6.1.0
