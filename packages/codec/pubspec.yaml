name: codec
description: "Type-safe, composable JSON codecs for Dart: structured CodecException with $.path on failure. Nested objects, discriminated unions, recursion, multi-version compatibility."
version: 0.2.1
repository: https://github.com/Vincen-dev/codec/tree/main/packages/codec
issue_tracker: https://github.com/Vincen-dev/codec/issues

topics:
  - json
  - codec
  - serialization
  - deserialization

environment:
  sdk: ^3.6.0

resolution: workspace

dev_dependencies:
  test: ^1.25.0
  lints: ^6.1.0
